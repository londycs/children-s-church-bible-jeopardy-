import React from 'react'
import ReactDOM from 'react-dom/client'
import BibleJeopardy from './bible-jeopardy.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <BibleJeopardy />
  </React.StrictMode>,
)
